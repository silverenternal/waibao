import type { ReactNode } from "react";
export interface TimelineItem { id:string; title:string; description?:string; time?:string; icon?:ReactNode }
export function Timeline({items}:{items:TimelineItem[]}){return <ol className="relative ml-3 border-s border-border">{items.map((item)=><li key={item.id} className="mb-8 ms-6 last:mb-0"><span className="absolute -start-3 flex size-6 items-center justify-center rounded-full bg-primary text-primary-foreground">{item.icon??<span className="size-2 rounded-full bg-current"/>}</span><div className="flex flex-wrap justify-between gap-2"><h3 className="font-semibold">{item.title}</h3>{item.time&&<time className="text-xs text-muted-foreground">{item.time}</time>}</div>{item.description&&<p className="mt-1 text-sm text-muted-foreground">{item.description}</p>}</li>)}</ol>}
