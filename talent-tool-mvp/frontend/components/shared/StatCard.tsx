import { Card } from "@tremor/react"; import { ArrowDownRight,ArrowUpRight,Minus } from "lucide-react"; import type { ReactNode } from "react";
export function StatCard({title,value,change,trend="neutral",icon}:{title:string;value:string|number;change?:string;trend?:"up"|"down"|"neutral";icon?:ReactNode}){const I=trend==="up"?ArrowUpRight:trend==="down"?ArrowDownRight:Minus;return <Card className="border-border bg-card text-card-foreground"><div className="flex justify-between"><p className="text-sm text-muted-foreground">{title}</p>{icon}</div><p className="mt-2 text-3xl font-semibold">{value}</p>{change&&<p className={`mt-2 flex items-center gap-1 text-sm ${trend==="down"?"text-red-500":trend==="up"?"text-emerald-500":"text-muted-foreground"}`}><I className="size-4"/>{change}</p>}</Card>}
