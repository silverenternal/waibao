import {Avatar,AvatarFallback,AvatarImage} from "@/components/ui/avatar"; import {cn} from "@/lib/utils"; export function UserAvatar({name,imageUrl,status="offline",className}:{name:string;imageUrl?:string;status?:"online"|"away"|"busy"|"offline";className?:string}){const initials=name.split(/\s+/).map(x=>x[0]).join("").slice(0,2).toUpperCase();const c={online:"bg-emerald-500",away:"bg-amber-500",busy:"bg-red-500",offline:"bg-slate-400"}[status];return <span className={cn("relative inline-flex",className)}><Avatar><AvatarImage src={imageUrl} alt={name}/><AvatarFallback>{initials}</AvatarFallback></Avatar><span aria-label={status} className={cn("absolute bottom-0 right-0 size-3 rounded-full border-2 border-background",c)}/></span>}
