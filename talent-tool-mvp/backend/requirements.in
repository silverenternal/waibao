# v10.0 T5019 — pip-tools input file.
#
# Edit THIS file to add/upgrade a direct dependency, then regenerate the
# lock with:
#
#     ./scripts/lock_requirements.sh          # or: pip-compile --generate-hashes
#
# ``requirements.txt`` is the pinned top-level set; ``requirements.lock`` is the
# fully-resolved, hash-pinned transitive closure used by CI / Docker for
# reproducible installs.

fastapi
uvicorn[standard]
pydantic
pydantic-settings
supabase
openai
httpx
python-jose[cryptography]
python-multipart
pgvector
psycopg2-binary
sqlalchemy
Levenshtein
pytest
pytest-asyncio

# T2701: RAG (LlamaIndex + Qdrant)
llama-index>=0.12.0
qdrant-client>=1.12.0
sentence-transformers>=3.0.0

# T2803: Predictive analytics (LightGBM + Prophet)
lightgbm>=4.0
prophet>=1.1
scikit-learn>=1.3
numpy>=1.24

# T2901: SSO/SAML (Authlib + python3-saml)
authlib>=1.3
python3-saml>=1.16
defusedxml>=0.7

# T5017: security tooling (runtime dependency for SSRF/rate-limit already stdlib)
slowapi
